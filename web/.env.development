# Dev-only env vars for Vite. Values are baked into the build at dev
# time. Loaded automatically by Vite when running `npm run dev`.
#
# See /brain/projects/research/sysdesign-lab/identity-provider/dev_workflow.md
# for the full story.

VITE_IDP_URL=http://localhost:8080
VITE_CLIENT_ID=localdev
