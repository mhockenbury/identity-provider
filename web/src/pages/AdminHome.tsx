// Admin placeholder — full implementation needs the IdP admin JSON
// API (task 15) to land first. This stub exists so the nav link goes
// somewhere.

export function AdminHome() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">admin</h1>
      <p className="text-gray-600">
        Not yet available. The IdP admin JSON API lands in task 15, then
        this page gets users / groups / outbox management.
      </p>
    </div>
  );
}
