package main

// Demo API entrypoint. Validates JWTs locally via JWKS against a configurable
// set of trusted issuers, enforces scope, checks FGA for fine-grained authz.
// Implementation pending.

func main() {}
